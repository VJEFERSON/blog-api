export interface ICreateTag {
    title: string;
    description: number;
    color: string;
}

export interface ITag {
    id: number;
    title: string;
    description: number;
    color: string;
}
